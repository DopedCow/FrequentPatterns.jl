"""
    fpgrowth(transactions, support, confidence, lift)

Returns a set of rules found in the transactions. If values for support, confidence or lift are provided, only rules that are above those thresholds will be included.
"""
function fpgrowth(transactions, support, confidence, lift)
  return rules
end
